{
  "buildCommand": "npm --prefix RadarFrontend install && npm --prefix RadarFrontend run build",
  "outputDirectory": "RadarFrontend/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
